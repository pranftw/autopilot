# Multi-Module Example

Demonstrates training with multiple AutoPilot modules composed together.

## Quick start

```bash
uv sync
uv run python run_trainer.py --max-epochs 3
```

## Scripts

- `run_trainer.py` -- train via `Trainer.fit()`
- `run.py` -- wrapper around `run_trainer.py`
