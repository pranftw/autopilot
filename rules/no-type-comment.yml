id: no-type-comment
language: Python
severity: error
message: "Do not use '# type:' inline annotations. Use real type annotations instead."
rule:
  kind: comment
  all:
    - regex: "^#\\s*type:"
    - not:
        regex: "^#\\s*type:\\s*ignore"
